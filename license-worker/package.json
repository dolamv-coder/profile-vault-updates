{
  "name": "orbit-license-worker",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "db:init": "wrangler d1 execute orbit-license --remote --file=schema.sql",
    "test": "node test/e2e.mjs"
  },
  "devDependencies": {
    "wrangler": "^4.141.0"
  }
}
